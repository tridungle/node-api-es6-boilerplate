/*
|--------------------------------------------------------------------------
| Application Constants
|--------------------------------------------------------------------------
|
| List of available constants.
|
*/
const constants = {
  database: {
    connectionMode: {
      URI: 'URI',
      CREDENTIALS: 'CREDENTIALS'
    }
  }
}

export default constants
